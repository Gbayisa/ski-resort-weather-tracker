# Base URL for the backend API.
# Leave blank when running locally with `npm run dev` (Vite proxy handles it).
# On Render (or any static hosting), set this to your backend service URL, e.g.:
#   VITE_API_BASE_URL=https://ski-resort-weather-tracker-api.onrender.com/api
VITE_API_BASE_URL=
